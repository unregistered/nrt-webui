<div class="navbar">
  <div class="navbar-inner">
    <a class="brand" href="#">NRT::Designer v{{App.VERSION}}</a>
	
	<ul class="nav pull-right">
	  <li class="dropdown">
	    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
			{{#if App.router.serverController.content.name}}
			{{App.router.serverController.content.name}}
			{{else}}
			Disconnected
			{{/if}}
	      <b class="caret"></b>
	    </a>
		<ul class="dropdown-menu" role="menu">
		{{#each server in App.router.serversController}}
        <li><a tabindex="-1" {{action viewServer server}}>{{server.name}}</a></li>
		{{/each}}
        <li class="divider"></li>
        <li><a tabindex="-1" href="#">Edit Servers</a></li>
		</ul>
	  </li>
	</ul>
  </div>
</div>

<div id="main" role="main" class="container-fluid">
  {{outlet}}
</div>
